import styled from "styled-components";
import {Heading} from "@c/UI/Heading";

const SkillProgressStyled = styled.div``

const Title = styled(Heading)`
    margin-bottom: 20px;
    padding: 0 10px;
`
const List = styled.div`
    display: grid;
    row-gap: 15px;
`

export {SkillProgressStyled}
export const S = {
    Title, List
}