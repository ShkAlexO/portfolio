import {Link} from "react-router-dom";

import styled from "styled-components";
import {RoundIconStyles} from "@c/ComponentStyles";

export const SocialItemStyled = styled(Link)`
    ${RoundIconStyles};
`